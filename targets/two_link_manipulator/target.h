#ifndef TARGET_H
#define TARGET_H

// the configured options and settings

#define TARGET_NAME "${TARGET_NAME}"
#define TARGET_NAMESPACE ${TARGET_NAME}
#define TARGET_SOURCE_DIR "${TARGET_SOURCE_DIR}"
#define TARGET_BINARY_DIR "${TARGET_BINARY_DIR}"
#define TARGET_INSTALL_DIR "${TARGET_INSTALL_DIR}"

#include "app.h"

#endif // TARGET_H